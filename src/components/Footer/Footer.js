import React from "react"
import './Footer.scss'

export const Footer = () => {
    return(
        <div className="footer">
            The footer is always pressed to the bottom
        </div>
    )
};